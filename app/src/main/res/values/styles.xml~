<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Базовые стили -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/background_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:fontFamily">@font/inter</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <!-- Стиль для чипов -->
    <style name="PreferenceChip" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="android:textColor">@color/chip_text</item>
        <item name="chipBackgroundColor">@color/chip_background</item>
        <item name="chipStrokeColor">@color/chip_stroke</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="rippleColor">@color/chip_background_selected</item>
        <item name="checkedIcon">@null</item>
        <item name="android:checkable">true</item>
    </style>

    <!-- Стиль для выбранных чипов -->
    <style name="PreferenceChip.Selected" parent="PreferenceChip">
        <item name="android:textColor">@color/chip_text_selected</item>
        <item name="chipBackgroundColor">@color/chip_background_selected</item>
        <item name="chipStrokeColor">@color/chip_stroke_selected</item>
        <item name="chipStrokeWidth">1dp</item>
    </style>

    <!-- Стиль для групп чипов -->
    <style name="PreferenceChipGroup" parent="Widget.MaterialComponents.ChipGroup">
        <item name="chipSpacingHorizontal">8dp</item>
        <item name="chipSpacingVertical">8dp</item>
        <item name="singleLine">false</item>
    </style>

    <!-- Стиль для заголовков секций -->
    <style name="SectionTitle">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:fontFamily">sans-serif-bold</item>
        <item name="android:layout_marginTop">24dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <!-- Стиль для подзаголовков -->
    <style name="SubsectionTitle">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <!-- Стиль для кнопок -->
    <style name="DuolingoButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/button_text</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">sans-serif-bold</item>
        <item name="android:letterSpacing">0.05</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <!-- Стиль для основной кнопки -->
    <style name="DuolingoButton.Primary" parent="DuolingoButton">
        <item name="backgroundTint">@color/button_primary</item>
        <item name="rippleColor">@color/button_primary_pressed</item>
    </style>

    <!-- Стиль для вторичной кнопки -->
    <style name="DuolingoButton.Secondary" parent="DuolingoButton">
        <item name="backgroundTint">@color/button_secondary</item>
        <item name="rippleColor">@color/button_secondary_pressed</item>
    </style>

    <!-- Стиль для карточек -->
    <style name="DuolingoCard" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/background_primary</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">2dp</item>
        <item name="contentPadding">16dp</item>
    </style>

    <!-- Стиль для диалогов -->
    <style name="DialogTheme" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:windowBackground">@drawable/dialog_background</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <!-- Custom Spinner Style -->
    <style name="CustomSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:background">@drawable/edittext_background</item>
        <item name="android:padding">16dp</item>
        <item name="android:textColor">@color/text_light</item>
        <item name="android:fontFamily">@font/inter_semibold</item>
        <item name="android:textSize">16sp</item>
        <item name="android:overlapAnchor">true</item>
    </style>

    <!-- Custom Spinner Dropdown Style -->
    <style name="CustomSpinnerDropDownStyle" parent="Widget.AppCompat.Spinner.Underlined">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:padding">16dp</item>
        <item name="android:textColor">@color/text_light</item>
        <item name="android:fontFamily">@font/inter_semibold</item>
        <item name="android:textSize">16sp</item>
        <item name="android:overlapAnchor">true</item>
        <item name="android:dropDownListViewStyle">@style/CustomSpinnerListViewStyle</item>
    </style>

    <!-- Custom Spinner ListView Style -->
    <style name="CustomSpinnerListViewStyle" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:divider">@null</item>
        <item name="android:dividerHeight">12dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:background">@color/background_primary</item>
    </style>

    <style name="CircleImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_up</item>
        <item name="android:windowExitAnimation">@anim/slide_down</item>
    </style>

    <!-- Custom dialog style -->
    <style name="CustomDialog" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
    </style>

    <!-- Стиль для Slider уровней сложности -->
    <style name="DifficultySlider" parent="Widget.MaterialComponents.Slider">
        <item name="tickVisible">true</item>
        <item name="tickColor">@color/text_light</item>
        <item name="tickColorActive">@color/primary</item>
        <item name="trackHeight">4dp</item>
        <item name="trackColor">@color/text_light</item>
        <item name="trackColorActive">@color/primary</item>
        <item name="thumbColor">@color/primary</item>
        <item name="thumbRadius">6dp</item>
        <item name="haloColor">@color/primary</item>
    </style>

    <style name="GrammarOptionButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">14sp</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Text Styles -->
    <style name="TextAppearance.LangUp.Headline1" parent="TextAppearance.MaterialComponents.Headline1">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:fontFamily">@font/inter_bold</item>
    </style>

    <style name="TextAppearance.LangUp.Headline2" parent="TextAppearance.MaterialComponents.Headline2">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:fontFamily">@font/inter_semibold</item>
    </style>

    <style name="TextAppearance.LangUp.Body1" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:fontFamily">@font/inter</item>
    </style>

    <style name="TextAppearance.LangUp.Body2" parent="TextAppearance.MaterialComponents.Body2">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:fontFamily">@font/inter</item>
    </style>

    <!-- Button Styles -->
    <style name="Button.LangUp" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/button_text</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/inter_bold</item>
        <item name="android:letterSpacing">0.05</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <style name="Button.LangUp.Primary" parent="Button.LangUp">
        <item name="backgroundTint">@color/button_primary</item>
        <item name="rippleColor">@color/button_primary_pressed</item>
    </style>

    <style name="Button.LangUp.Secondary" parent="Button.LangUp">
        <item name="backgroundTint">@color/button_secondary</item>
        <item name="rippleColor">@color/button_secondary_pressed</item>
    </style>

    <style name="Button.LangUp.Text" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/text_blue</item>
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/inter_medium</item>
        <item name="rippleColor">@color/chip_background_selected</item>
    </style>

    <!-- Card Styles -->
    <style name="Card.LangUp" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/background_primary</item>
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">2dp</item>
        <item name="contentPadding">16dp</item>
        <item name="strokeColor">@color/border_default</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <!-- Input Styles -->
    <style name="TextInputLayout.LangUp" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/text_blue</item>
        <item name="boxStrokeWidth">1dp</item>
        <item name="android:textColorHint">@color/text_secondary</item>
        <item name="hintTextColor">@color/text_blue</item>
        <item name="errorTextColor">@color/error</item>
    </style>

    <style name="TextInputEditText.LangUp" parent="Widget.MaterialComponents.TextInputEditText.OutlinedBox">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/inter</item>
    </style>

    <!-- Chip Styles -->
    <style name="Chip.LangUp" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="android:textColor">@color/chip_text</item>
        <item name="chipBackgroundColor">@color/chip_background</item>
        <item name="chipStrokeColor">@color/chip_stroke</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/inter_medium</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="rippleColor">@color/chip_background_selected</item>
        <item name="checkedIcon">@null</item>
    </style>

    <style name="ChipGroup.LangUp" parent="Widget.MaterialComponents.ChipGroup">
        <item name="chipSpacingHorizontal">8dp</item>
        <item name="chipSpacingVertical">8dp</item>
        <item name="singleLine">false</item>
    </style>

    <!-- Dialog Styles -->
    <style name="Dialog.LangUp" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:windowBackground">@drawable/dialog_background</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <!-- List Styles -->
    <style name="ListView.LangUp" parent="Widget.AppCompat.ListView">
        <item name="android:divider">@color/divider</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:background">@color/background_primary</item>
    </style>

    <!-- Progress Bar Styles -->
    <style name="ProgressBar.LangUp" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressBackgroundTint">@color/background_tertiary</item>
        <item name="android:progressTint">@color/primary</item>
        <item name="android:minHeight">4dp</item>
    </style>

    <!-- Bottom Navigation Styles -->
    <style name="BottomNavigation.LangUp" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@color/text_primary</item>
        <item name="itemTextColor">@color/text_primary</item>
        <item name="itemRippleColor">@color/chip_background_selected</item>
        <item name="itemTextAppearanceActive">@style/TextAppearance.LangUp.Body2</item>
        <item name="itemTextAppearanceInactive">@style/TextAppearance.LangUp.Body2</item>
    </style>

    <!-- Toolbar Styles -->
    <style name="Toolbar.LangUp" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:background">@color/background_primary</item>
        <item name="titleTextColor">@color/text_primary</item>
        <item name="subtitleTextColor">@color/text_secondary</item>
        <item name="titleTextAppearance">@style/TextAppearance.LangUp.Headline2</item>
        <item name="subtitleTextAppearance">@style/TextAppearance.LangUp.Body2</item>
    </style>
</resources> 